import { FC, ReactElement } from "react";
import './time-picker.scss';

const TimePicker: FC = (): ReactElement => {
    return (
      <div className="time-picker">
      </div>
    )
};
export default TimePicker;