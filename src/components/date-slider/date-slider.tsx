import { FC, ReactElement } from "react";
import './date-slider.scss';

const DateSlider: FC = (): ReactElement => {
    return (
      <div className="date-slider">
      </div>
    )
};
export default DateSlider;